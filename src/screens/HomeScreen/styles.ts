import {ViewProps} from 'react-native';
import styled from 'styled-components/native';
import {color} from '../../assets/tokens/colors';

export const WrapperHome = styled.View<ViewProps>`
  flex: 1;
  background-color: ${color.blue[40]};
`;
