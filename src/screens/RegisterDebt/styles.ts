import styled from 'styled-components/native';
import {color} from '../../assets/tokens/colors';

export const RegisterDebtWrapper = styled.ScrollView`
  flex: 1;
  background-color: ${color.white[30]};
`;
